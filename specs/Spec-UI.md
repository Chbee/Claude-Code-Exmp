# TravelCalculator 기획서 — 화면 설계 & 디자인 시스템

> 2차 인터뷰 기반 최종 확정본 | 2026-04-05
> 관련: [기능 명세](Spec-Overview.md) | [아키텍처](Spec-Architecture.md) | [데이터 모델](Spec-DataModel.md) | [태스크](Spec-Tasks.md)

---

## 3. 화면 설계

### 3.1 계산기 화면 (메인)

```
┌─────────────────────────────────┐
│  [🇺🇸 USD ▼]    ●              │  ← Toolbar (카메라/설정 V1 숨김)
├─────────────────────────────────┤
│  ┌─ 오프라인 배너 (오프라인 시) ─┐ │
│  │ 오프라인 — 04/04 14:00 기준  │ │
│  └────────────────────────────┘ │
│    1 USD = 1,350.00 KRW   [↻]  │  ← 환율 정보 + 새로고침
│                                 │
│        USD  1,000.00           │  ← 입력 금액 (출발 통화)
│              [↓]               │  ← 방향 전환 버튼 (탭 가능)
│        KRW  1,350,000          │  ← 변환 금액 (도착 통화)
│                                 │
├─────────────────────────────────┤
│  [AC/C]  [←]   [ ÷ ]          │  ← 키패드 1행
│   [ 7]   [ 8]  [ 9]  [ × ]    │
│   [ 4]   [ 5]  [ 6]  [ - ]    │
│   [ 1]   [ 2]  [ 3]  [ + ]    │
│   [   0   0  ] [ . ]  [ = ]   │
└─────────────────────────────────┘
```

#### Toolbar 구성
- 통화 선택 버튼 (pill 형태, 국기 + 통화코드)
- 온/오프라인 상태 인디케이터 (소형 ●)
- 카메라/설정 버튼: **V1에서 숨김** (레이아웃 자리는 유지, opacity=0)

#### Display 영역
- 오프라인 배너: 환율 정보 영역 바로 위, 절대 시간 병기 (오프라인 시에만 표시)
- 환율 정보 + 새로고침: `"1 USD = 1,350.00 KRW"` + 우측 ↻ 버튼
- 입력 금액: 통화 기호 + 천단위 콤마 포맷
- 방향 전환 버튼 `↓`: 탭 시 결과값을 새 입력값으로 이전하며 방향 전환
- 변환 금액: 통화 기호 + 천단위 콤마 포맷, 통화별 소수점 자릿수 적용
- 긴 숫자는 폰트 크기 자동 축소 (`minimumScaleFactor`)

### 3.2 통화 선택 화면

```
┌─────────────────────────────────┐
│           여행 통화 설정      [X] │  ← 타이틀 + 닫기
│     통화 설정을 위해 국가 선택    │  ← 서브타이틀
├─────────────────────────────────┤
│  [📍 현재 위치로 자동 설정]       │  ← 위치 기반 선택
├─────────────────────────────────┤
│  🇰🇷 대한민국                    │
│     KRW                    ✓   │  ← 현재 선택됨
├─────────────────────────────────┤
│  🇺🇸 미국                       │
│     USD                        │
├─────────────────────────────────┤
│  🇹🇼 대만                       │
│     TWD                        │
└─────────────────────────────────┘
```

#### 플로우
1. Toolbar의 통화 버튼 탭 → fullScreenCover 모달 표시
2. 통화 직접 선택 또는 위치 자동 설정
3. 위치 버튼:
   - 권한 `.notDetermined` → 시스템 권한 팝업
   - 권한 `.denied` → Toast(info) + iOS 설정 앱 딥링크 안내
   - 권한 `.granted` → GPS → 역지오코딩 → 통화 자동 선택
4. 결과 Toast 표시 (성공 / 미지원 지역 / 에러)
5. 선택 시 모달 자동 닫힘 + 계산기 리셋

### 3.3 온보딩 화면
- 통화 선택 화면과 동일한 UI 재사용 (CurrencySelectView, `isOnboarding: Bool`)
- X 버튼 비노출 (강제 선택)
- **KRW 제외** — USD, TWD만 표시
- 위치 버튼 유지 (한국 감지 시 Toast 안내, 여행지 감지 시 자동 선택)
- 타이틀: "여행지 통화를 선택해주세요"

### 3.4 전체 화면 에러 상태
- 트리거: API 실패 + 캐시 없음
- 에러 아이콘 + 에러 메시지 + 재시도 버튼
- 계산기 키패드 비활성화

---

## 6. 디자인 시스템

### 6.1 컬러 팔레트

#### Main Color Scale (10단계)
Main050, Main100, Main200, Main300, Main400, Main500, Main600, Main700, Main800, Main900

#### Gray Scale (10단계)
Gray050, Gray100, Gray200, Gray300, Gray400, Gray500, Gray600, Gray700, Gray800, Gray900

#### Accent Colors
Blue500, Green500, Red500, Yellow500, Black500, White500

### 6.2 아이콘
- camera, settings, map-pin
- wifi, wifi-off
- x (닫기)
- arrow-down-arrow-up (방향 전환 버튼)
- arrow.clockwise (새로고침)
- Toast 아이콘: ToastSuccess, ToastError, ToastWarning, ToastInfo

### 6.3 햅틱 피드백
| 상황 | 타입 |
|------|------|
| 입력 자릿수 초과 | warning (notification) |
| Toast 표시 | light (impact) |
| 방향 전환 버튼 탭 | medium (impact) |
| 통화 선택 완료 | success (notification) |
| 등호(=) 결과 표시 | light (impact) |
